; Native Plugin - CPU execution backend
; Uses OCaml 5 Domains for parallelism

(library
 (name sarek_native)
 (libraries
  unix
  ctypes
  spoc_framework
  spoc_framework_registry
  sarek
  (re_export spoc_core))
 (modules Native_plugin_v2)
 (preprocess no_preprocessing))
