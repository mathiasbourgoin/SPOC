; Native Plugin - CPU execution backend
; Uses OCaml 5 Domains for parallelism

(library
 (name sarek_native)
 (libraries
  unix
  ctypes
  sarek_framework
  sarek
  (re_export sarek_core))
 (modules Native_plugin Native_plugin_v2)
 (preprocess no_preprocessing))
