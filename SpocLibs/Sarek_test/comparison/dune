(executable
  (name test_reference_ir)
  (libraries str)
)

(rule
  (alias runtest)
  (deps
    ../regression/reference_ir.txt)
  (action
    (chdir %{project_root}
      (run %{exe:test_reference_ir.exe})))
)
