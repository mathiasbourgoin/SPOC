(dirs common unit comparison regression e2e negative native new_runtime)

(alias
 (name runtest)
 (deps
  (:unit
   (alias unit/runtest))
  (:comparison
   (alias comparison/runtest))
  (:e2e
   (alias e2e/runtest))))
